/* import { Model, Op } from "sequelize";
import { LicenceSchema } from "./schemas/LicenceSchema.js";

class LicenceModel extends Model {}

export class Licence extends LicenceSchema {
  constructor() {
    super();
    this.licence = LicenceModel;
    this.licence.init(this.schema, {
      ...this.config,
      primaryKey: "product_id",
    });
  }
}
 */
